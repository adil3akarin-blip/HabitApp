# TASK 8 — HabitDetailsScreen + CalendarMonth

Implement TASK 8 only.

## Objective
Show habit details with monthly calendar; tap a day to toggle completion.

## Requirements
Create:
- `src/components/CalendarMonth.tsx`

Update:
- `src/screens/HabitDetailsScreen.tsx`

Calendar requirements:
- Monthly grid with weekday headers
- Highlight days that are in the habit’s `activeDates` set
- Tap day toggles completion (calls store `toggleDate`)
- Prev/Next month controls

Also show:
- Icon + name
- Current streak (can be placeholder number until TASK 9, but prefer to wire compute function if ready)

## Definition of Done
- Tap on any day toggles highlight
- Home grid updates accordingly

## Output format (MANDATORY)
1) ✅ TASK 8 done
2) Files changed/added
3) How to verify
