TASK
Add “amazing but evergreen” SVG-based animations to my Expo React Native app. The result must feel premium (Duolingo-level feedback) but timeless (no trendy gimmicks).

CONSTRAINTS
- Expo managed workflow.
- Use: react-native-svg, react-native-reanimated, (gesture-handler only if needed).
- Prefer evergreen motion: subtle physics, short durations, clarity-first.
- 60fps: animate on UI thread, avoid JS per-frame loops.
- Implement reduced motion support (single toggle or system preference hook).
- Provide clean TypeScript, reusable components, and usage examples.
- Do NOT break existing UI; integrate safely and incrementally.
- If you must add a dependency, justify it and keep it minimal.

OUTPUT FORMAT (must follow)
1) “Moment Map” (where/when animations happen in the app)
2) Motion tokens (durations/springs/stagger defaults)
3) Components to add (with file paths)
4) Code (complete TS/TSX files)
5) Integration instructions (what to change in existing screens)
6) Performance & accessibility checklist

ANIMATIONS TO IMPLEMENT (minimum set)
A) Press feedback wrapper for SVG icons and cards (press depth + subtle scale)
B) SVG “draw-on” checkmark (strokeDasharray/strokeDashoffset)
C) ProgressRing with animated progress (dashoffset)
D) Stagger list item reveal (subtle fade + translateY)
E) One reward moment for milestones (micro burst: e.g., small sparkle SVG that fades/scales; rare, not noisy)

PROJECT INPUT
I will paste my current folder structure and the relevant screens/components. If I don’t, assume a standard Expo Router app:
- /app/(tabs)/home.tsx
- /app/(tabs)/settings.tsx
- /src/ui/
- /src/features/

IMPLEMENTATION REQUIREMENTS
- Create a small SVG motion kit:
  - /src/ui/motion/tokens.ts
  - /src/ui/svg/CheckDraw.tsx
  - /src/ui/svg/ProgressRing.tsx
  - /src/ui/svg/SparkleBurst.tsx (simple evergreen reward)
  - /src/ui/press/PressFeedback.tsx
  - /src/ui/list/StaggerItem.tsx (or StaggerList.tsx)
- Each component must be themeable: size, color, strokeWidth, style.
- Use React.memo where appropriate.
- Use Animated.createAnimatedComponent + useAnimatedProps for SVG attribute animation.
- Provide example usage snippets for:
  - “Complete habit” interaction (checkmark draw-on + mild haptic suggestion)
  - Header progress ring
  - List with stagger items
  - Milestone reward moment

QUALITY BAR
- No large complex morphing unless requested.
- Avoid heavy blur/overdraw.
- Keep code readable and production-ready.
- Comment only where it prevents mistakes (avoid noise).

Start by proposing a concise Moment Map based on a typical habit-tracker app. Then generate the full implementation as specified.
